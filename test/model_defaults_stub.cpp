#include "global.hpp"

// Minimal stub data for Model class - just enough to link tests
// These are much shorter versions - tests don't need the full training data
const std::vector<float> default_pos_data = {0.5f, 0.4f, 0.3f, 0.2f, 0.1f};
const std::vector<float> default_neg_data = {0.1f, 0.05f, 0.02f, 0.01f, 0.005f};
